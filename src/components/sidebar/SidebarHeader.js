import { Box, styled } from '@mui/material'
import React from 'react'

const StyledbarHeader = styled(Box)(()=>({
    padding:"0 15px",
    height: "64px",
    display: "flex",
    alignItems: "center",
}))
export const SidebarHeader = () => {
  return <StyledbarHeader>ssss</StyledbarHeader>
}
